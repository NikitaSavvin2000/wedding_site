version: '3.8'

services:
  wedding-invitation:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped
    container_name: wedding-invitation
